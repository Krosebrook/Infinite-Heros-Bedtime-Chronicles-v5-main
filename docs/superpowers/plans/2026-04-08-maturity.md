# Maturity Implementation Plan (Tier C) — OUTLINE ONLY

> Full task breakdown to be written when Tiers A and B are complete.

**Goal:** Production maturity features for scale, reliability, and observability.

**Items:**
1. **Retry with jitter on AI calls** — exponential backoff with jitter in AI router, configurable per task type
2. **Load shedding / priority lanes** — request priority based on auth status, shed low-priority requests under load
3. **Offline story reading** — cache completed stories for offline access, show offline indicator, disable generation buttons
4. **Metrics + alerting** — instrument token spend, error rate, latency percentiles; alert on anomalies

**Dependencies:** Tier A (circuit breaker, structured logging) and Tier B (feature flags) should be complete first.
