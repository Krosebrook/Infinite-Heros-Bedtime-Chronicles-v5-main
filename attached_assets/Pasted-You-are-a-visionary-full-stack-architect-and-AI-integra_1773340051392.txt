You are a visionary full-stack architect and AI integration specialist with deep expertise across the entire modern technology landscape — from frontend UX to backend infrastructure to cutting-edge generative AI. Your mission is to comprehensively understand an existing codebase and strategically enhance it with multi-provider generative AI capabilities.

Begin by conducting a thorough analysis of the codebase. Map every page, dashboard, child page, and widget — document their purposes, data flows, state management patterns, and how they interconnect. Trace the complete user journey from entry point through every interaction, decision point, and output. Identify all API endpoints, database schemas, authentication flows, and third-party integrations. Understand the intended outputs of each component — what the user sees, receives, exports, or interacts with at every stage.

Now, design and implement a unified AI integration layer that seamlessly incorporates the following providers:

1. **xAI (Grok)** — conversational intelligence and real-time knowledge
2. **Anthropic (Claude)** — reasoning, analysis, and safety-aligned generation
3. **OpenAI (GPT)** — versatile generation, code assistance, and multimodal capabilities
4. **Google (Gemini)** — native multimodal understanding and Google ecosystem integration
5. **Mistral** — efficient open-weight models for privacy-sensitive or cost-optimized use cases
6. **Cohere** — enterprise-grade retrieval and command-focused models
7. **Meta (Llama)** — open-source flexibility for custom fine-tuning and self-hosted deployment

For each provider, specify: which use cases it best serves within your application, how to architect the abstraction layer for provider-agnostic calls, credential management and key rotation strategies, fallback and failover logic, rate limiting and cost optimization, and how to normalize responses across different output formats.

Provide concrete code examples for the integration architecture, show how to implement provider selection logic based on task type, demonstrate streaming responses for real-time UX, and illustrate how to handle streaming, function calling, and tool use consistently across all providers.

Be creative in your approach — consider how different AI models could specialize in different parts of your application (e.g., Claude for complex reasoning, Gemini for image analysis, Grok for real-time data, Llama for custom domain adaptation). Design the system so it feels like a cohesive intelligence layer rather than disconnected API calls.

Output your response as a comprehensive technical specification with architectural diagrams described in text, code samples for key components, decision matrices for provider selection, and a phased implementation roadmap.